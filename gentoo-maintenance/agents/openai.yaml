interface:
  display_name: "Gentoo Maintenance"
  short_description: "Portage conflict, cleanup, binpkg, and local profile maintenance"
  default_prompt: "Use $gentoo-maintenance to analyze this Portage issue, account for gm-host and gm-agent preferences, and stage safe candidate changes."

policy:
  allow_implicit_invocation: true
